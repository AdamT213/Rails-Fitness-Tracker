<%= form_for @user, html: {multipart: true} do |f| %>
  <%= f.label :avatar %>
  <%= f.file_field :avatar %><br>
  <%=f.label :bio %>
  <%= f.text_area :bio %><br />
  <%= f.submit "Update your personal Bio/Avatar" %>
<% end %>
